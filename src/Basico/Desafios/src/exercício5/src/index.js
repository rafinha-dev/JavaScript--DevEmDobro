// primeiro : um for que conte até 10
// o número precisa dobrar a cada repetição
// a mensagem deve conter qual o nomero atual e seu dobro

for (let index = 1; index <= 10 ; index++) {
    let dobro = index * 2
    console.log('O número atual é ' + index + ' e seu dobro é '+  index * 2)
    // seria mais limpo usar o nome da variavel ao invez de fazer o calculo direto
}